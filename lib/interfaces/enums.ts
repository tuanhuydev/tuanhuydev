export enum ProjectType {
  OUTSOURCE = "outsource",
  PRODUCT = "product",
  POC = "poc",
}

export enum ProjectStatus {
  PLAN = "plan",
  GOING = "going",
  CANCELLED = "cancelled",
  CLOSED = "closed",
}
