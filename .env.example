NEXT_PUBLIC_HOST=http://localhost:3000
NODE_ENV=development


# DATABASE
DATABASE_URL=postgresql://user:example@localhost:5432/tuanhuydev
MONGODB_URI=
MONGODB_DB=

# GOOGLE
GOOGLE_ADSENSE=
GOOGLE_TAG=

# AUTH (external auth service)
NEXT_PUBLIC_AUTH_URL=
# Generate strong, unique secrets per environment (e.g. `openssl rand -hex 32`).
# Never reuse the placeholder values below in any real deployment.
TOKEN_SECRET=
TOKEN_LIFE=3600
REFRESH_TOKEN_SECRET=
REFRESH_TOKEN_LIFE=7200
ACCESS_TOKEN_SECRET=
ACCESS_TOKEN_LIFE=20m

