export const GENERATIVE_CHAT_SESSION_NAME_PROMPT = (prompt: string) =>
  `Generate a very short, concise title (5 words maximum) for a chat session that starts with this prompt: "${prompt}". Just return the title, nothing else.`;
